# sandbox

The real running-example pipeline lives here — built in **Phase 1**, not
Phase 0. It will hold the synthetic asset-pricing study (seed-fixed generator,
clean/merge, one long-short portfolio, one FF5 regression, one table, one
figure) wired together by a `Makefile` (PLAN.md Sections 7 and 7.1).

Empty at G0 by design; this README just reserves the top-level home.
