# transcript
chapter: 06
os: Ubuntu 22.04.5 LTS (workspace sandbox)
shell: bash 5.1.16
tool: GNU coreutils 8.32
date: 2026-06-30
captured-by: sandbox
note: a glob that matches nothing is passed through literally
      (default bash). shopt -s nullglob makes it expand to
      nothing instead. Run in /tmp.
---
$ ls *.csv
fy 2011.csv

$ ls *.xyz
ls: cannot access '*.xyz': No such file or directory
$ echo $?
2

$ shopt -s nullglob
$ echo "matches:[" *.xyz "]"
matches:[ ]
$ shopt -u nullglob
