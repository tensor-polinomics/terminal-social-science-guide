# transcript
chapter: 17
os: macOS 26.5.1 (Apple Silicon)
shell: zsh 5.9 (arm64-apple-darwin25.0)
tool: zsh (startup-file order)
date: 2026-07-07
captured-by: user-mac
note: Every zsh here runs against a THROWAWAY HOME (and
note: ZDOTDIR) with demo rc files; the real ~/.zshenv etc.
note: are never read. ZDOTDIR is pointed at the throwaway
note: too, because zsh reads $ZDOTDIR/.zshenv (not
note: $HOME/.zshenv) whenever ZDOTDIR is set. Interactive
note: zsh with no tty drops a job-control notice on stderr
note: with 2>/dev/null; a real Terminal tab has a tty and
note: never prints it. HOME/ZDOTDIR/$TMPDIR masked.
---

$ ls -a
. .. .zlogin .zprofile .zshenv .zshrc

$ zsh -l -i -c 'true' 2>/dev/null   # login + interactive
reading ~/.zshenv
reading ~/.zprofile
reading ~/.zshrc
reading ~/.zlogin

$ zsh -i -c 'true' 2>/dev/null      # non-login interactive
reading ~/.zshenv
reading ~/.zshrc

$ zsh -c 'true'                      # non-interactive script
reading ~/.zshenv
