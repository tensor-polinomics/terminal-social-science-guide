# transcript
chapter: 04
os: macOS 26.5.1 (Apple Silicon)
shell: zsh 5.9 (arm64-apple-darwin25.0) (login default)
tool: BSD ln/readlink/realpath (macOS base)
date: 2026-07-02
captured-by: user-mac
note: `ln -s` and `readlink` behave like the GNU side. On
note: recent macOS `readlink -f` and `realpath` BOTH resolve
note: (older macOS lacked `readlink -f`, and `realpath`
note: arrived relatively recently), so both are run and their
note: real result recorded for this machine (26.5.1: exit 0).
note: Owner masked to [account]. The resolved absolute paths
note: sit under the macOS per-account $TMPDIR, whose folder
note: hash is masked to /private/var/folders/[tmpdir] (a
note: machine identifier); the ch04mac.XXXXXX suffix is a
note: random mktemp tag, not identifying.
---

$ ls -l proj/latest_factors.csv
lrwxr-xr-x@ 1 [account]  staff  20 Jul  2 13:45 proj/latest_factors.csv -> data/raw/factors.csv

$ readlink proj/latest_factors.csv
data/raw/factors.csv

$ readlink -f proj/latest_factors.csv; echo "exit=$?"
/private/var/folders/[tmpdir]/T/ch04mac.Xfxp18/proj/data/raw/factors.csv
exit=0

$ realpath proj/latest_factors.csv; echo "exit=$?"
/private/var/folders/[tmpdir]/T/ch04mac.Xfxp18/proj/data/raw/factors.csv
exit=0
