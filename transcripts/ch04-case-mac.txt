# transcript
chapter: 04
os: macOS 26.5.1 (Apple Silicon)
shell: zsh 5.9 (arm64-apple-darwin25.0) (login default)
tool: APFS default (case-insensitive)
date: 2026-07-02
captured-by: user-mac
note: The default macOS volume is case-INSENSITIVE (but case-
note: preserving): Foo.txt and foo.txt name the SAME file, so
note: writing foo.txt overwrites Foo.txt and `ls` shows one
note: entry. On Linux (ch04-case.txt) they are two files. If
note: this Mac uses a case-sensitive volume the demo will show
note: two files instead; that is recorded honestly either way.
note: No user data in these lines.
---

$ touch Foo.txt
$ printf 'lower\n' > foo.txt
$ ls
Foo.txt
$ cat Foo.txt
lower
