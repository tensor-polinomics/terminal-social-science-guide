# transcript
chapter: 16
os: macOS 26.5.1 (Apple Silicon)
shell: zsh (login default)
tool: Claude Code + OpenAI Codex + Gemini CLI
date: 2026-07-01
captured-by: user-mac
note: real evidence that the permission/approval/sandbox
      flags named in the chapter exist. The SEMANTICS are
      pinned to official docs (verification/chapter-16.md),
      not to this help text, which is version-volatile.
---

$ claude --help | grep -iE 'permission|dangerous'
  --allow-dangerously-skip-permissions  Enable bypassing all permission checks
  --dangerously-skip-permissions        Bypass all permission checks.
  --permission-mode <mode>              Permission mode to use for the session
                                        "bypassPermissions", "default",
                                        permissions work normally. Sets

$ codex --help | grep -iE 'sandbox|approval|approve|yolo'
  sandbox         Run commands within a Codex-provided sandbox
          'sandbox_permissions=["disk-full-read-access"]'` - `-c
  -s, --sandbox <SANDBOX_MODE>
          Select the sandbox policy to use when executing model-generated shell
      --dangerously-bypass-approvals-and-sandbox
          sandboxing. EXTREMELY DANGEROUS. Intended solely for running in
          environments that are externally sandboxed
  -a, --ask-for-approval <APPROVAL_POLICY>
          Configure when the model requires human approval before executing a
            asking for user approval. Will escalate to the user if the model
            approval. Only asks for approval if a command fails to execute, in
            which case it will escalate to the user to ask for un-sandboxed
          - on-request: The model decides when to ask the user for approval
          - never:      Never ask for user approval Execution failures are
          `web_search` tool is available to the model (no per‑call approval)

$ codex --yolo --version   # does the alias parse?
codex-cli 0.142.5

$ gemini --help | grep -iE 'approval|yolo|sandbox|allowed-tools'
  -s, --sandbox                   Run in sandbox?  [boolean]
  -y, --yolo                      Automatically accept all actions (aka YOLO mode, see https://www.youtube.com/watch?v=xvFZjo5PgG0 for more details)?  [boolean] [default: false]
      --approval-mode             Set the approval mode: default (prompt for approval), auto_edit (auto-approve edit tools), yolo (auto-approve all tools), plan (read-only mode)  [string] [choices: "default", "auto_edit", "yolo", "plan"]
      --allowed-tools             [DEPRECATED: Use Policy Engine instead See https://geminicli.com/docs/core/policy-engine] Tools that are allowed to run without confirmation  [array]
