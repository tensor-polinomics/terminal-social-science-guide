# transcript
chapter: 09
os: macOS 26.5.1 (Apple Silicon)
shell: zsh 5.9 (arm64-apple-darwin25.0) (login default)
tool: BSD ps (macOS base)
date: 2026-07-03
captured-by: user-mac
note: macOS ships BSD ps; Linux ships procps-ng. A secret in a
note: command's args is exposed by ps on both. ps aux (BSD form)
note: works on both platforms; ps -ef (System V) is the GNU
note: idiom. hunter2 is a fake placeholder. ps -o pid,args prints
note: no owner column, so nothing is masked.
---

$ ps -o pid,args -p "$!"
  PID ARGS
 9760 python3 -c import time; time.sleep(30) --password=hunter2
