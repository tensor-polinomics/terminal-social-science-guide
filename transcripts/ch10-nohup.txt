# transcript
chapter: 10
os: Ubuntu 22.04.5 (workspace sandbox)
shell: bash 5.1.16
tool: GNU coreutils 8.32 (nohup)
date: 2026-07-03
captured-by: sandbox
note: nohup with an explicit log redirect, the portable form.
  (nohup's default "appending output to nohup.out" behavior
  only triggers when stdout is a terminal, so it does not
  appear in this non-interactive capture; the explicit
  redirect is the recommended form anyway.) long_job.sh prints
  three steps one second apart. No user data; nothing masked.

The script (long_job.sh):

  #!/usr/bin/env bash
  for i in 1 2 3; do
    echo "step $i done"
    sleep 1
  done
---
$ nohup ./long_job.sh > build.log 2>&1 &
$ cat build.log
step 1 done
step 2 done
step 3 done
