# transcript
chapter: 18
os: macOS 26.5.1 (Apple Silicon)
shell: zsh 5.9 (arm64-apple-darwin25.0)
tool: zsh (login/interactive state of a real terminal tab)
date: 2026-07-07
captured-by: user-mac
note: Run in a real Terminal.app tab. `[[ -o login ]]` is zsh's
note: canonical login-shell test; `echo $0` is a bash-only
note: heuristic (a leading '-') and does NOT reliably report
note: login state in zsh. A WezTerm tab returned the same
note: (login shell). No personal data in the output.
---

$ [[ -o login ]] && echo "login shell" || echo "not a login shell"
login shell

$ [[ -o interactive ]] && echo "interactive" || echo "not interactive"
interactive
