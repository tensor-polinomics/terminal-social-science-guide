# transcript
chapter: 04
os: Ubuntu 22.04.5 (workspace sandbox)
shell: bash 5.1.16
tool: GNU coreutils 8.32
date: 2026-07-02
captured-by: sandbox
note: run in a throwaway /tmp/ch04mk scratch. Shows plain
      mkdir failing on a missing parent, `mkdir -p` creating
      the whole chain, and `rmdir` removing an empty directory
      but refusing a non-empty one. No user-specific data.
      `exit=N` lines are added by the capture to show the exit
      code; a reader would read it with `echo $?`.
---
$ mkdir results
$ mkdir results/2026/figures
mkdir: cannot create directory ‘results/2026/figures’: No such file or directory
$ echo $?
1
$ mkdir -p results/2026/figures
$ echo $?
0
$ ls -R results
results:
2026

results/2026:
figures

results/2026/figures:
$ rmdir results/2026/figures
$ rmdir results
rmdir: failed to remove 'results': Directory not empty
