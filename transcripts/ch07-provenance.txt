# transcript
chapter: 07
os: Ubuntu 22.04.5 LTS (workspace sandbox)
shell: bash 5.1.16
tool: GNU coreutils 8.32
date: 2026-06-30
captured-by: sandbox
note: capturing a run's stdout+stderr to a log file for the
      record (the REPRODUCIBILITY / provenance point). Run in a
      /tmp copy of the pipeline's data so paths display cleanly.
---
$ { echo "run: distinct-firm count"; \
    cut -d, -f1 data/raw/firm_panel.csv \
      | tail -n +2 | sort -u | wc -l; } > run.log 2>&1
$ cat run.log
run: distinct-firm count
300
